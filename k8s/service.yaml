apiVersion: v1
kind: Service
metadata:
  name: sample-order-service
  namespace: staging
  labels:
    app: sample-order-service
spec:
  type: ClusterIP
  selector:
    app: sample-order-service
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
